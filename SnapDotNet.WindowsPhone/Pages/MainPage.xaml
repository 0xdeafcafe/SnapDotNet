<Page
    x:Class="SnapDotNet.Apps.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnapDotNet.Apps.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	>

	<Grid>
		<Grid.ColumnDefinitions>
			<!-- temp, will do proper UI later !-->
			<ColumnDefinition Width="10"/>
			<ColumnDefinition Width="1*"/>
			<ColumnDefinition Width="10"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="10"/>
			<RowDefinition Height="1*"/>
			<RowDefinition Height="10"/>
		</Grid.RowDefinitions>

		<Canvas
			Grid.Column="1" Grid.Row="1">
			<CaptureElement x:Name="CameraPreview" Width="320" Height="240"/>
		</Canvas>
	</Grid>
	<Page.BottomAppBar>
		<CommandBar>
			<AppBarButton x:Name="CameraTriggerButton" Label="Capture" Icon="Camera"/>
			<AppBarButton x:Name="CameraReverseButton" Label="Capture" Icon="Up"/>
			<CommandBar.SecondaryCommands>
				<!--AppBarButton x:Name="other" Label="unspecified" /-->
			</CommandBar.SecondaryCommands>
		</CommandBar>
	</Page.BottomAppBar>
</Page>
